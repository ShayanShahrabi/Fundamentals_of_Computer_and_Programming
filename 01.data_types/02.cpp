#include <iostream>

using namespace std;

int main(){
    // int a = 99;
    // float b = 99;
    // cout << a / 10 << endl;
    // cout << b / 10;

    // float t, f;
    float t;
    cin >> t;
    // f = (t * 9) / 5 + 32;
    cout << (t * 9) / 5 + 32 << " Fahrenheit";
    return 0;
}